import Button from "@renderer/Layout/Button";

const SettingsSidebar = (): JSX.Element => {
  return (
    <div className="flex flex-col gap-4 w-[20vw]">
        <Button theme="default">Main</Button>
    </div>
  )
};

export default SettingsSidebar;
